diff --git a/src/engine/encounter/combat/enemy/EnemyBehaviorRunner.ts b/src/engine/encounter/combat/enemy/EnemyBehaviorRunner.ts
index 93d27cd..4de1db3 100644
--- a/src/engine/encounter/combat/enemy/EnemyBehaviorRunner.ts
+++ b/src/engine/encounter/combat/enemy/EnemyBehaviorRunner.ts
@@ -7,7 +7,7 @@ type EnemyBehaviorRunnerOptions = {
 
     clearPlayerStickyMine: (mineId: string, targetActorId: string) => boolean;
 
-    deployEnemyShield?: (actor: ShipEncounterActorState) => void;
+    deployEnemyShield: (actor: ShipEncounterActorState) => void;
 
     random?: () => number;
 };
@@ -26,13 +26,7 @@ export default class EnemyBehaviorRunner {
         this.crewTaskRunner = new EnemyCrewTaskRunner({
             state: this.state,
 
-            onShieldDeploymentCompleted: (actor) => {
-                if (!deployEnemyShield) {
-                    throw new Error("Enemy shield deployment callback is missing");
-                }
-
-                deployEnemyShield(actor);
-            },
+            onShieldDeploymentCompleted: deployEnemyShield,
 
             onStickyMineClearingCompleted: (actor, mineId) => {
                 const cleared = clearPlayerStickyMine(mineId, actor.id);
diff --git a/src/engine/encounter/combat/enemy/EnemyCrewTaskRunner.ts b/src/engine/encounter/combat/enemy/EnemyCrewTaskRunner.ts
index d9ac445..f4ac3eb 100644
--- a/src/engine/encounter/combat/enemy/EnemyCrewTaskRunner.ts
+++ b/src/engine/encounter/combat/enemy/EnemyCrewTaskRunner.ts
@@ -3,7 +3,7 @@ import type { ShipEncounterActorState } from "../../actors/ship/ship_encounter_a
 type EnemyCrewTaskRunnerOptions = {
     state: EncounterState;
 
-    onShieldDeploymentCompleted?: (actor: ShipEncounterActorState) => void;
+    onShieldDeploymentCompleted: (actor: ShipEncounterActorState) => void;
 
     onStickyMineClearingCompleted: (actor: ShipEncounterActorState, mineId: string) => void;
 
@@ -27,11 +27,7 @@ export default class EnemyCrewTaskRunner {
     }: EnemyCrewTaskRunnerOptions) {
         this.state = state;
 
-        this.onShieldDeploymentCompleted =
-            onShieldDeploymentCompleted ??
-            (() => {
-                throw new Error("Enemy shield deployment callback is missing");
-            });
+        this.onShieldDeploymentCompleted = onShieldDeploymentCompleted;
 
         this.onStickyMineClearingCompleted = onStickyMineClearingCompleted;
 
diff --git a/tests/engine/encounter/enemy_crew_task_runner.test.ts b/tests/engine/encounter/enemy_crew_task_runner.test.ts
index 899bd83..5ac024e 100644
--- a/tests/engine/encounter/enemy_crew_task_runner.test.ts
+++ b/tests/engine/encounter/enemy_crew_task_runner.test.ts
@@ -2,6 +2,9 @@
         new EnemyCrewTaskRunner({
             state,
 
+            onShieldDeploymentCompleted:
+                () => {},
+
             onStickyMineClearingCompleted:
                 () => {},
 
