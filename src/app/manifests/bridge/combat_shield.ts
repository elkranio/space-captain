// src/app/manifests/bridge/combat_shield.ts

import {
    DEFAULT_ATLAS_KEY,
    type SpriteEntry,
} from '../types';

export const PLAYER_SHIELD_CENTER_SPRITE:
    SpriteEntry = {
        atlasKey:
            DEFAULT_ATLAS_KEY,

        frameKey:
            'combat/shields/center_00',
    };
