diff --git a/src/app/scenes/game/bridge/view/combat/BridgeCombatView.ts b/src/app/scenes/game/bridge/view/combat/BridgeCombatView.ts
index 0805348..dfe64d6 100644
--- a/src/app/scenes/game/bridge/view/combat/BridgeCombatView.ts
+++ b/src/app/scenes/game/bridge/view/combat/BridgeCombatView.ts
@@ -61,101 +61,87 @@ export default class BridgeCombatView {
     ) {}
 
     public prepare(): void {
+        const getObjectPosition = (objectId: string) => {
+            return this.spaceView.getObjectPosition(objectId);
+        };
+
+        const getObjectVisualBounds = (objectId: string) => {
+            return this.spaceView.getObjectVisualBounds(objectId);
+        };
+
+        const setObjectPresentationOffsetX = (objectId: string, offsetX: number) => {
+            return this.spaceView.setObjectPresentationOffsetX(objectId, offsetX);
+        };
+
         this.incomingMissilesView = new BridgeIncomingMissilesView(
             this.scene,
             this.eventBus,
 
-            (objectId) => {
-                return this.spaceView.getObjectPosition(objectId);
-            },
+            getObjectPosition,
         );
 
         this.outgoingMissilesView = new BridgeOutgoingMissilesView(
             this.scene,
             this.eventBus,
 
-            (objectId) => {
-                return this.spaceView.getObjectPosition(objectId);
-            },
+            getObjectPosition,
 
-            (objectId) => {
-                return this.spaceView.getObjectVisualBounds(objectId);
-            },
+            getObjectVisualBounds,
         );
 
         this.outgoingStickyMinesView = new BridgeOutgoingStickyMinesView(
             this.scene,
             this.eventBus,
 
-            (objectId) => {
-                return this.spaceView.getObjectPosition(objectId);
-            },
+            getObjectPosition,
 
-            (objectId) => {
-                return this.spaceView.getObjectVisualBounds(objectId);
-            },
+            getObjectVisualBounds,
         );
 
         this.outgoingSpamView = new BridgeOutgoingSpamView(
             this.scene,
             this.eventBus,
 
-            (objectId) => {
-                return this.spaceView.getObjectVisualBounds(objectId);
-            },
+            getObjectVisualBounds,
         );
 
         this.beamCannonThreatsView = new BridgeBeamCannonThreatsView(
             this.scene,
             this.eventBus,
 
-            (objectId) => {
-                return this.spaceView.getObjectPosition(objectId);
-            },
+            getObjectPosition,
         );
 
         this.beamCannonBeamsView = new BridgeBeamCannonBeamsView(
             this.scene,
             this.eventBus,
 
-            (objectId) => {
-                return this.spaceView.getObjectPosition(objectId);
-            },
+            getObjectPosition,
         );
 
         this.enemyShieldView = new BridgeEnemyShieldView(
             this.scene,
             this.eventBus,
 
-            (objectId) => {
-                return this.spaceView.getObjectPosition(objectId);
-            },
+            getObjectPosition,
         );
 
         this.enemyEvadeView = new BridgeEnemyEvadeView(
             this.scene,
             this.eventBus,
 
-            (objectId) => {
-                return this.spaceView.getObjectVisualBounds(objectId);
-            },
+            getObjectVisualBounds,
 
-            (objectId, offsetX) => {
-                return this.spaceView.setObjectPresentationOffsetX(objectId, offsetX);
-            },
+            setObjectPresentationOffsetX,
         );
 
         this.playerBeamCannonView = new BridgePlayerBeamCannonView(
             this.scene,
             this.eventBus,
 
-            (objectId) => {
-                return this.spaceView.getObjectPosition(objectId);
-            },
+            getObjectPosition,
 
-            (objectId) => {
-                return this.spaceView.getObjectVisualBounds(objectId);
-            },
+            getObjectVisualBounds,
         );
 
         this.playerShieldView = new BridgePlayerShieldView(this.scene, this.eventBus);
@@ -164,9 +150,7 @@ export default class BridgeCombatView {
             this.scene,
             this.eventBus,
 
-            (objectId) => {
-                return this.spaceView.getObjectPosition(objectId);
-            },
+            getObjectPosition,
         );
 
         this.vfxView = new BridgeVfxView(this.scene, this.eventBus);
@@ -177,9 +161,7 @@ export default class BridgeCombatView {
             this.scene,
             this.eventBus,
 
-            (objectId) => {
-                return this.spaceView.getObjectPosition(objectId);
-            },
+            getObjectPosition,
         );
 
         // Created last so near-camera Evade dust remains above physical
