import fs from "node:fs";

const path = "tests/app/BridgeStickyMineEventContract.test.ts";
const expectedHead = "ce412b559693f856b422b9a0169fffc64972934d";

const checkOnly = process.argv.includes("--check");
const apply = process.argv.includes("--apply");

if (checkOnly === apply) {
    console.error("Usage: node fix-sticky-mine-event-contract-exact.mjs --check|--apply");
    process.exit(1);
}

const expected = "// tests/app/BridgeStickyMineEventContract.test.ts\n\nimport {\n    describe,\n    expect,\n    it,\n    vi,\n} from 'vitest';\nimport BridgeEncounterEngineEventHandler from '../../src/app/scenes/game/bridge/controller/encounter/engine_events/BridgeEncounterEngineEventHandler';\nimport {\n    BRIDGE_EVENT,\n    BRIDGE_STICKY_MINE_REMOVAL_OUTCOME,\n} from '../../src/app/scenes/game/bridge/events/bridge_event';\nimport type BridgeEventBus from '../../src/app/scenes/game/bridge/events/BridgeEventBus';\nimport {\n    OFFICER_ROLE,\n} from '../../src/engine/defs/officer';\nimport {\n    COMBAT_SOURCE_KIND,\n    COMBAT_TARGET_KIND,\n} from '../../src/engine/encounter/model/combat';\nimport {\n    ENCOUNTER_EVENT,\n    OFFICER_TASK_OUTCOME,\n    OFFICER_TASK_RESULT_KIND,\n    type EncounterEvent,\n} from '../../src/engine/encounter/model/event';\n\ndescribe('Bridge sticky-mine event contract', () => {\n    it('maps attach, clear and detonation to bridge lifecycle', () => {\n        const emit = vi.fn();\n\n        const handler =\n            new BridgeEncounterEngineEventHandler(\n                {\n                    emit,\n                } as unknown as BridgeEventBus,\n            );\n\n        handler.handle(\n            {\n                type:\n                    ENCOUNTER_EVENT\n                        .STICKY_MINE_ATTACHED,\n\n                mine: createMine(\n                    'mine_1',\n                    7500,\n                ),\n            }\n        );\n\n        handler.handle(\n            {\n                type:\n                    ENCOUNTER_EVENT\n                        .OFFICER_TASK_ENDED,\n\n                task: {\n                    role: OFFICER_ROLE.SCIENCE,\n                },\n\n                outcome:\n                    OFFICER_TASK_OUTCOME\n                        .COMPLETED,\n\n                result: {\n                    kind:\n                        OFFICER_TASK_RESULT_KIND\n                            .STICKY_MINE_CLEARED,\n\n                    mineId: 'mine_1',\n                },\n            }\n        );\n\n        handler.handle(\n            {\n                type:\n                    ENCOUNTER_EVENT\n                        .STICKY_MINE_DETONATED,\n\n                mine: createMine(\n                    'mine_2',\n                    0,\n                ),\n\n                appliedDamage: 1,\n                remainingHull: 2,\n                destroyed: false,\n            }\n        );\n\n        expect(emit.mock.calls).toEqual([\n            [\n                BRIDGE_EVENT\n                    .STICKY_MINE_ADDED,\n\n                {\n                    mineId: 'mine_1',\n                    sourceActorId: 'enemy',\n                    initialTimeToDetonationMs:\n                        7500,\n                },\n            ],\n\n            [\n                BRIDGE_EVENT\n                    .STICKY_MINE_REMOVED,\n\n                {\n                    mineId: 'mine_1',\n\n                    outcome:\n                        BRIDGE_STICKY_MINE_REMOVAL_OUTCOME\n                            .CLEARED,\n                },\n            ],\n\n            [\n                BRIDGE_EVENT\n                    .STICKY_MINE_REMOVED,\n\n                {\n                    mineId: 'mine_2',\n\n                    outcome:\n                        BRIDGE_STICKY_MINE_REMOVAL_OUTCOME\n                            .DETONATED,\n                },\n            ],\n\n        ]);\n    });\n});\n\nfunction createMine(\n    id: string,\n    timeToDetonationMs: number,\n) {\n    return {\n        id,\n\n\n        source: {\n            kind:\n                COMBAT_SOURCE_KIND.ACTOR,\n\n            actorId: 'enemy',\n        },\n\n        sourceWeaponId: 'dispenser',\n\n        target: {\n            kind:\n                COMBAT_TARGET_KIND\n                    .PLAYER_SHIP,\n        },\n\n        timeToDetonationMs,\n        initialTimeToDetonationMs: 7500,\n\n        damage: 1,\n    };\n}\n";
const replacement = "// tests/app/BridgeStickyMineEventContract.test.ts\n\nimport {\n    describe,\n    expect,\n    it,\n    vi,\n} from 'vitest';\nimport BridgeEncounterEngineEventHandler from '../../src/app/scenes/game/bridge/controller/encounter/engine_events/BridgeEncounterEngineEventHandler';\nimport {\n    BRIDGE_EVENT,\n    BRIDGE_STICKY_MINE_REMOVAL_OUTCOME,\n} from '../../src/app/scenes/game/bridge/events/bridge_event';\nimport type BridgeEventBus from '../../src/app/scenes/game/bridge/events/BridgeEventBus';\nimport {\n    OFFICER_ROLE,\n} from '../../src/engine/defs/officer';\nimport {\n    COMBAT_SOURCE_KIND,\n    COMBAT_TARGET_KIND,\n} from '../../src/engine/encounter/model/combat';\nimport {\n    ENCOUNTER_OFFICER_COMMAND_ID,\n} from '../../src/engine/encounter/model/command';\nimport {\n    ENCOUNTER_EVENT,\n    OFFICER_TASK_OUTCOME,\n    OFFICER_TASK_RESULT_KIND,\n} from '../../src/engine/encounter/model/event';\nimport {\n    OFFICER_TASK_KIND,\n} from '../../src/engine/encounter/model/officer_task';\n\ndescribe('Bridge sticky-mine event contract', () => {\n    it('maps attach, clear and detonation to bridge lifecycle', () => {\n        const emit = vi.fn();\n\n        const handler =\n            new BridgeEncounterEngineEventHandler(\n                {\n                    emit,\n                } as unknown as BridgeEventBus,\n            );\n\n        handler.handle(\n            {\n                type:\n                    ENCOUNTER_EVENT\n                        .STICKY_MINE_ATTACHED,\n\n                mine: createMine(\n                    'mine_1',\n                    7500,\n                ),\n            }\n        );\n\n        handler.handle(\n            {\n                type:\n                    ENCOUNTER_EVENT\n                        .OFFICER_TASK_ENDED,\n\n                task: {\n                    id: 'task_clear_mine_1',\n\n                    kind:\n                        OFFICER_TASK_KIND\n                            .CLEAR_STICKY_MINE,\n\n                    role: OFFICER_ROLE.SCIENCE,\n\n                    sourceCommandId:\n                        ENCOUNTER_OFFICER_COMMAND_ID\n                            .CLEAR_STICKY_MINE,\n\n                    mineId: 'mine_1',\n\n                    label: 'CLEAR MINE',\n                    showProgress: true,\n\n                    durationMs: 3000,\n                    elapsedMs: 3000,\n\n                    canBeCancelledByPlayer: true,\n                    canBeInterruptedByDamage: true,\n                },\n\n                outcome:\n                    OFFICER_TASK_OUTCOME\n                        .COMPLETED,\n\n                result: {\n                    kind:\n                        OFFICER_TASK_RESULT_KIND\n                            .STICKY_MINE_CLEARED,\n\n                    mineId: 'mine_1',\n                },\n            }\n        );\n\n        handler.handle(\n            {\n                type:\n                    ENCOUNTER_EVENT\n                        .STICKY_MINE_DETONATED,\n\n                mine: createMine(\n                    'mine_2',\n                    0,\n                ),\n\n                appliedDamage: 1,\n                remainingHull: 2,\n                destroyed: false,\n            }\n        );\n\n        expect(emit.mock.calls).toEqual([\n            [\n                BRIDGE_EVENT\n                    .STICKY_MINE_ADDED,\n\n                {\n                    mineId: 'mine_1',\n                    sourceActorId: 'enemy',\n                    initialTimeToDetonationMs:\n                        7500,\n                },\n            ],\n\n            [\n                BRIDGE_EVENT\n                    .STICKY_MINE_REMOVED,\n\n                {\n                    mineId: 'mine_1',\n\n                    outcome:\n                        BRIDGE_STICKY_MINE_REMOVAL_OUTCOME\n                            .CLEARED,\n                },\n            ],\n\n            [\n                BRIDGE_EVENT\n                    .STICKY_MINE_REMOVED,\n\n                {\n                    mineId: 'mine_2',\n\n                    outcome:\n                        BRIDGE_STICKY_MINE_REMOVAL_OUTCOME\n                            .DETONATED,\n                },\n            ],\n\n        ]);\n    });\n});\n\nfunction createMine(\n    id: string,\n    timeToDetonationMs: number,\n) {\n    return {\n        id,\n\n\n        source: {\n            kind:\n                COMBAT_SOURCE_KIND.ACTOR,\n\n            actorId: 'enemy',\n        },\n\n        sourceWeaponId: 'dispenser',\n\n        target: {\n            kind:\n                COMBAT_TARGET_KIND\n                    .PLAYER_SHIP,\n        },\n\n        timeToDetonationMs,\n        initialTimeToDetonationMs: 7500,\n\n        damage: 1,\n    };\n}\n";

const raw = fs.readFileSync(path, "utf8");
const newline = raw.includes("\r\n") ? "\r\n" : "\n";
const normalized = raw.replace(/\r\n/g, "\n");

if (normalized !== expected) {
    console.error(
        "ERROR: current file does not match exact preimage from master " +
        expectedHead + ". Nothing changed."
    );
    process.exit(1);
}

console.log(
    "OK: exact preimage matches master " + expectedHead +
    " (CRLF/LF ignored)."
);

if (checkOnly) {
    console.log("CHECK ONLY: no files changed.");
    process.exit(0);
}

fs.writeFileSync(path, replacement.replace(/\n/g, newline), "utf8");
console.log("APPLIED: fixed BridgeStickyMineEventContract.test.ts.");
